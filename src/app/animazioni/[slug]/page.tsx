import Link from 'next/link';import { animations } from '@/data/animations';import { notFound } from 'next/navigation';
export default function D({params}:{params:{slug:string}}){const a=animations.find(x=>x.slug===params.slug); if(!a) return notFound(); return <main className='containerx py-10 space-y-3'><h1 className='text-4xl'>{a.title}</h1><p>{a.description}</p><p>Tipo: {a.type}</p><p>Parametri: {a.parameters.join(', ')}</p><p>Questa simulazione può essere eseguita localmente scaricando il file dalla cartella indicata. In futuro potrà essere convertita o incorporata come canvas/iframe.</p>{a.downloadUrl&&<Link className='underline' href={a.downloadUrl}>Scarica simulazione</Link>} {a.embedUrl&&<Link className='underline' href={a.embedUrl}>Apri embed</Link>}<ul>{a.launchInstructions.map(i=><li key={i}>• {i}</li>)}</ul></main>}
